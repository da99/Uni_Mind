#!/usr/bin/env ruby
# -*- ruby -*-

require 'Uni_Mind' 

name = File.basename File.expand_path('.')
args = [name] + ARGV
arg  = File.join *args

puts "", 'Executing: ', arg

require "./#{name}"
app = Uni_Mind.new( arg )
app.fulfill

at_exit {
  puts ""
}

