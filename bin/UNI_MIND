#!/usr/bin/env ruby
# -*- ruby -*-

require 'Uni_Mind' 

args = if ARGV.size > 1
        '/' + ARGV.join('/')
       else
         ARGV.first
       end

puts "", 'Executing: ', args

app = Uni_Mind.new( args )
app.fulfill_request

at_exit {
  puts ""
}

