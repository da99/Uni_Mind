#!/usr/bin/env ruby
# -*- ruby -*-

path  = File.join *ARGV
puts "", 'Executing: ', path

require 'Uni_Mind' 
require "./#{kname = File.basename File.expand_path('.')}"
klass = Object.const_get(kname)
app = klass.new( path )
app.fulfill

at_exit {
  puts ""
}

