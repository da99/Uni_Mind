#!/usr/bin/env ruby
# -*- ruby -*-

require 'Uni_Mind' 

if ARGV.size > 1
  raise "Unknown arguments: #{ARGV[1,ARGV.size]}"
end

puts "", 'Executing: ', ARGV.first

app = Uni_Mind.new( ARGV.first )
app.fulfill_request

at_exit {
  puts ""
}

